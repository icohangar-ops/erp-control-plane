"""GenBI answer promotion (spec §4.2): NL answer -> governed Superset artifact.

A second caller of the v0.1 idempotent Superset provisioning loop — guardrailed
execution against the READ_ONLY DuckDB, deterministic slugs, create-or-update
persistence, audited outcomes, and a coverage-request queue for "not modeled yet".
"""
