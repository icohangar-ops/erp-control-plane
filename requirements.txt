# Lean runtime for serverless deployment (api/index.py demo API only).
# The Vercel Python runtime installs this file for the deployment; full
# development dependencies live in pyproject.toml (pip install -e ".[dev]").
fastapi==0.141.1
duckdb==1.5.5
