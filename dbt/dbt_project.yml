name: bmd_erp_control_plane
version: 0.1.0
config-version: 2
profile: bmd_analytics

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  bmd_erp_control_plane:
    staging:
      +materialized: view
      +schema: staging
    canonical:
      +materialized: table
      +schema: canonical
    marts:
      +materialized: view
      +schema: marts

seeds:
  bmd_erp_control_plane:
    +schema: reference
